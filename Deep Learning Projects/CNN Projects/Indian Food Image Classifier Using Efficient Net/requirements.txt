# Runtime dependencies for the Streamlit inference app (CPU-friendly).
# For a CPU-only install of torch/torchvision:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
streamlit>=1.36
torch>=2.2
torchvision>=0.17
Pillow>=10.0
numpy>=1.26
